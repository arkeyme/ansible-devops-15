version: '3'
services:
  redis:
    image: redis
    volumes:
      redis_data:/var/lib/redis/
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports: 
      - 8080:80
volumes:
  redis_data:
    driver: local
